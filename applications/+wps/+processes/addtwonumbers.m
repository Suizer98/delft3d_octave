function [ c ] = addtwonumbers( a, b)
%ADDTWONUMBERS add two numbers, for testing WPS

% This is also part of the abstract
%
% input:
% a = double
% b = double
%
% output:
% c = double
c = double(a) + double(b);

end
