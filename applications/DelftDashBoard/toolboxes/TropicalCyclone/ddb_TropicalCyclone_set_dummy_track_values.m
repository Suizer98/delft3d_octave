function track=ddb_TropicalCyclone_set_dummy_track_values(nt)

% Set dummy values
track.time=zeros([nt 1]);
track.x=zeros([nt 1]);
track.y=zeros([nt 1]);
track.vmax=zeros([nt 1])-999;
track.pc=zeros([nt 1])-999;
track.rmax=zeros([nt 1])-999;
track.r35ne=zeros([nt 1])-999;
track.r35se=zeros([nt 1])-999;
track.r35sw=zeros([nt 1])-999;
track.r35nw=zeros([nt 1])-999;
track.r50ne=zeros([nt 1])-999;
track.r50se=zeros([nt 1])-999;
track.r50sw=zeros([nt 1])-999;
track.r50nw=zeros([nt 1])-999;
track.r65ne=zeros([nt 1])-999;
track.r65se=zeros([nt 1])-999;
track.r65sw=zeros([nt 1])-999;
track.r65nw=zeros([nt 1])-999;
track.r100ne=zeros([nt 1])-999;
track.r100se=zeros([nt 1])-999;
track.r100sw=zeros([nt 1])-999;
track.r100nw=zeros([nt 1])-999;
